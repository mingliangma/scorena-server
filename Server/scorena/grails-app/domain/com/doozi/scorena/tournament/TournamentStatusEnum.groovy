package com.doozi.scorena.tournament

/**
 * @author mingliangma
 *
 * 	NEW: The tournament is not begun
 *	ACTIVE: the tournament is started
 *	EXPIRED: the tournament is ended
 */
public enum TournamentStatusEnum {
	NEW, ACTIVE, EXPIRED
}
